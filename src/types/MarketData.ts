export default interface MarketData {
    id: string;
    title: string;
    description: string;
    creator: string;
    endDate: string;
    category: string;
    volume: string;
    liquidity: string;
    yesPrice: string;
    noPrice: string;
    resolved: boolean;
    outcome: string
}